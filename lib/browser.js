var React = require('react');
//var WidgetHolder = React.createFactory(require("./WidgetHolder"));

module.exports = function(initalState, domId){
  //var r = React.render(WidgetHolder(initalState), document.getElementById(domId).parentNode)
  //console.log('widget ', domId, 'loaded');
  //return r;
}
